# Returnly Environment Configuration

# Basic Configuration
NODE_ENV=development
SESSION_SECRET=your-session-secret-here
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key

# Database Configuration
DATABASE_URL=your-postgresql-database-url

# Google OAuth Configuration (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Environment Access Control
# To disable public access, set NODE_ENV=production
# Or customize with JSON configuration:
# RETURNLY_ENV_CONFIG={"allowPublicRegistration":false,"allowPublicLogin":false,"allowGoogleAuth":false,"allowDriverSignup":false,"enableDemoMode":true,"restrictToWhitelist":true,"whitelistedEmails":["nabeelmumtaz92@gmail.com"]}

# Production Environment Example:
# NODE_ENV=production
# This automatically disables public registration, login, Google auth, and driver signup
# Only whitelisted emails (nabeelmumtaz92@gmail.com) can access the platform for mobile app testing

# Development Environment (default):
# NODE_ENV=development  
# Allows all authentication methods and features for testing

# Mobile App Testing Configuration:
# The account nabeelmumtaz92@gmail.com is whitelisted for exclusive mobile app testing access

# Staging Environment Protection (for returnly.tech)
STAGING_USERNAME=returnly
STAGING_PASSWORD=staging2025