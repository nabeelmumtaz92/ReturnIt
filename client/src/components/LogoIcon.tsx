import React from 'react';

// Re-export LogoIcon components from ReturnItLogo for backward compatibility
export { ReturnItLogo, ReturnItIcon } from './ReturnItLogo';

// Export LogoIcon as an alias for ReturnItIcon for backward compatibility
export { ReturnItIcon as LogoIcon } from './ReturnItLogo';